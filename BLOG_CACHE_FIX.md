# 🔧 دليل حل مشكلة عدم ظهور المقالات

## المشكلة
المقالات الجديدة لا تظهر في صفحة المدونة أو لوحة التحكم رغم وجودها في قاعدة البيانات.

## السبب
نظام الـ **Caching** يحفظ المقالات لمدة **30 دقيقة**، لذلك المقالات الجديدة لا تظهر فوراً.

---

## ✅ الحلول (تم تطبيقها)

### 1. إنشاء API لمسح الـ Cache
تم إنشاء `/api/revalidate` لمسح الـ cache يدوياً أو تلقائياً.

### 2. مسح تلقائي عند الحفظ
لوحة التحكم الآن تمسح الـ cache تلقائياً عند:
- ✅ إضافة مقال جديد
- ✅ تعديل مقال موجود
- ✅ حذف مقال

### 3. إصلاح حقل الصورة
تم تصحيح اسم حقل الصورة من `image` إلى `featuredImage` في صفحة المدونة.

### 4. إنشاء API للمحتوى
تم إنشاء `/api/content` لقراءة محتوى الصفحات من قاعدة البيانات.

---

## 🚀 كيفية الاستخدام

### الطريقة 1: مسح الـ Cache تلقائياً (موصى بها)
لا تحتاج لفعل شيء! عند إضافة أو تعديل مقال من لوحة التحكم، الـ cache يُمسح تلقائياً.

### الطريقة 2: مسح الـ Cache يدوياً (إذا لزم الأمر)

#### من المتصفح:
افتح الرابط التالي:
\`\`\`
http://localhost:3000/api/revalidate?tag=blog
\`\`\`

#### من Terminal:
\`\`\`bash
# مسح cache المدونة فقط
node scripts/clear-cache.js

# أو استخدم curl
curl http://localhost:3000/api/revalidate?tag=blog
\`\`\`

### الطريقة 3: إعادة تشغيل الـ Server
إذا لم تنجح الطرق السابقة:
\`\`\`bash
# أوقف السيرفر (Ctrl+C)
# ثم شغله من جديد
npm run dev
\`\`\`

---

## 📋 التحقق من المقالات

### فحص قاعدة البيانات:
\`\`\`bash
node scripts/check-blog-posts.js
\`\`\`

### فحص محتوى صفحة المدونة:
\`\`\`bash
node scripts/check-blog-content.js
\`\`\`

---

## 🔍 الأوامر المفيدة

### مسح كل أنواع الـ Cache:
\`\`\`bash
# من المتصفح
http://localhost:3000/api/revalidate?tag=all

# من Terminal
curl http://localhost:3000/api/revalidate?tag=all
\`\`\`

### مسح cache أنواع محددة:
\`\`\`bash
# المدونة
http://localhost:3000/api/revalidate?tag=blog

# المنتجات
http://localhost:3000/api/revalidate?tag=products

# الإعدادات
http://localhost:3000/api/revalidate?tag=settings
\`\`\`

---

## 🎯 ملاحظات مهمة

### ⚠️ تأكد من:
1. تشغيل الـ development server: `npm run dev`
2. عدم وجود أخطاء في console المتصفح
3. الدخول على الصفحة الصحيحة: `/ar/blog` أو `/en/blog`

### 💡 نصيحة:
في **Production**، الـ cache مفيد للأداء. لكن في **Development**، قد تحتاج لمسحه أحياناً.

---

## 🐛 استكشاف الأخطاء

### المشكلة: المقال موجود في القاعدة لكن غير ظاهر
**الحل:**
1. امسح الـ cache: `node scripts/clear-cache.js`
2. حدّث الصفحة (F5)
3. افحص console المتصفح للأخطاء

### المشكلة: الصورة لا تظهر
**الحل:**
1. تأكد من وجود الصورة في `public/images/`
2. أو ارفعها من لوحة التحكم
3. المسار يجب أن يبدأ بـ `/` مثل: `/images/granite.jpg`

### المشكلة: لوحة التحكم لا تظهر المقالات
**الحل:**
1. تحقق من تسجيل الدخول
2. افحص قاعدة البيانات: `node scripts/check-blog-posts.js`
3. افحص console المتصفح

---

## 📊 الملفات التي تم تعديلها

1. ✅ `/src/app/api/revalidate/route.ts` - جديد
2. ✅ `/src/app/api/content/route.ts` - جديد
3. ✅ `/src/app/admin/blog/page.tsx` - تعديل (مسح cache تلقائي)
4. ✅ `/src/app/[locale]/blog/page.tsx` - تعديل (featuredImage)
5. ✅ `/scripts/clear-cache.js` - جديد

---

## 🎉 الخلاصة

الآن المقالات **تظهر فوراً** بعد الإضافة أو التعديل لأن:
- ✅ الـ cache يُمسح تلقائياً
- ✅ API المحتوى يعمل بشكل صحيح
- ✅ حقل الصورة تم إصلاحه

**لا تحتاج لعمل أي شيء يدوياً!** 🚀
