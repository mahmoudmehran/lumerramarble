# إضافة اللوجو والعلامة التجارية (Logo & Branding Setup)

## 📋 نظرة عامة
تمت إضافة نظام كامل لإدارة اللوجو والعلامة التجارية في الموقع مع دعم:
- رفع لوجو الشركة
- دعم لوجو منفصل للوضع الليلي (Dark Mode)
- نصوص بديلة بـ 4 لغات (عربي، إنجليزي، إسباني، فرنسي)
- Favicon للمتصفح
- معاينة مباشرة في صفحة الإعدادات

---

## 🔧 التغييرات التقنية

### 1. Database Schema (Prisma)
**الملف:** `prisma/schema.prisma`

**الحقول المضافة:**
```prisma
// Logo & Branding
logoUrl           String?
logoAlt           String   @default("Company Logo")
logoAltAr         String   @default("شعار الشركة")
logoAltEs         String   @default("Logo de la Empresa")
logoAltFr         String   @default("Logo de l'Entreprise")
darkModeLogoUrl   String?  // Optional separate logo for dark mode
faviconUrl        String?
```

**Migration:**
```bash
npx prisma migrate dev --name add_logo_branding
```

**الحالة:** ✅ تم التنفيذ بنجاح

---

### 2. Admin Settings Page
**الملف:** `src/app/admin/settings/page.tsx`

**قسم جديد مضاف:**
```tsx
{/* Logo & Branding */}
<Card className="p-6">
  <h2>الشعار والعلامة التجارية</h2>
  
  {/* رفع مباشر بدون الحاجة لصفحة منفصلة */}
  <ImageUpload
    currentImages={settings.logoUrl ? [settings.logoUrl] : []}
    onUpload={(urls) => setSettings({...prev, logoUrl: urls[0]})}
    onRemove={() => setSettings({...prev, logoUrl: ''})}
    multiple={false}
    maxFiles={1}
  />
</Card>
```

**Features:**
- ✅ رفع اللوجو مباشرة (Drag & Drop أو Click)
- ✅ معاينة فورية للصورة المرفوعة
- ✅ زر حذف سهل (❌)
- ✅ دعم 3 شعارات: عادي، ليلي، favicon
- ✅ نص بديل بـ 4 لغات
- ✅ معاينة مقسمة (وضع عادي + وضع ليلي)
- ✅ رسائل توجيهية

---

### 3. TypeScript Interface
**الملف:** `src/lib/settings.ts`

**التحديث:**
```typescript
export interface SiteSettings {
  // Company Information
  companyName: string
  companyNameAr: string
  companyNameEs: string
  companyNameFr: string
  
  // Logo & Branding
  logoUrl?: string
  logoAlt?: string
  logoAltAr?: string
  logoAltEs?: string
  logoAltFr?: string
  darkModeLogoUrl?: string
  faviconUrl?: string
  
  description: string
  // ...rest
}
```

**getSiteSettings() Function:**
تم تحديث الدالة لإرجاع حقول اللوجو من قاعدة البيانات:
```typescript
logoUrl: settings.logoUrl || undefined,
logoAlt: settings.logoAlt || undefined,
logoAltAr: settings.logoAltAr || undefined,
logoAltEs: settings.logoAltEs || undefined,
logoAltFr: settings.logoAltFr || undefined,
darkModeLogoUrl: settings.darkModeLogoUrl || undefined,
faviconUrl: settings.faviconUrl || undefined,
```

---

### 4. Navbar Component
**الملف:** `src/components/layout/Navbar.tsx`

**التحديثات:**
```tsx
import Image from 'next/image'

{/* Show Logo if available, otherwise show fallback */}
{siteSettings?.logoUrl ? (
  <div className="relative w-10 h-10">
    <Image
      src={siteSettings.darkModeEnabled && siteSettings.darkModeLogoUrl 
        ? siteSettings.darkModeLogoUrl 
        : siteSettings.logoUrl
      }
      alt={locale === 'ar' ? siteSettings.logoAltAr : ...}
      fill
      className="object-contain"
      priority
    />
  </div>
) : (
  // Fallback: Gradient with "L" letter
  <div className="w-10 h-10 bg-gradient...">
    <span>L</span>
  </div>
)}
```

**Features:**
- ✅ عرض اللوجو من الإعدادات
- ✅ دعم تبديل تلقائي بين لوجو عادي ولوجو الوضع الليلي
- ✅ نص بديل حسب اللغة المختارة
- ✅ Fallback (احتياطي) إذا لم يتم رفع لوجو
- ✅ استخدام Next.js Image للتحسين التلقائي
- ✅ عرض اسم الشركة من الإعدادات حسب اللغة

---

### 5. Footer Component
**الملف:** `src/components/layout/Footer.tsx`

**التحديثات:**
```tsx
import Image from 'next/image'
import { useSiteSettings } from '../../contexts/ThemeContext'

const siteSettings = useSiteSettings()

{/* Same logo logic as Navbar */}
{siteSettings?.logoUrl ? (
  <Image ... />
) : (
  <div>L</div> // Fallback
)}
```

**Features:**
- ✅ نفس منطق Navbar
- ✅ لوجو أكبر (w-12 h-12 بدلاً من w-10 h-10)
- ✅ اتساق في العرض عبر الموقع

---

## 📝 كيفية الاستخدام

### ✨ رفع مباشر بالسحب والإفلات (Drag & Drop)

#### خطوة 1: افتح صفحة الإعدادات
1. اذهب إلى: **Admin Panel → الإعدادات**
2. ابحث عن قسم **"الشعار والعلامة التجارية"**

#### خطوة 2: ارفع اللوجو
**طريقتان للرفع:**

**أ) السحب والإفلات:**
- اسحب ملف اللوجو من جهازك
- أفلته في المنطقة المخصصة (مكتوب "اسحب الصور هنا أو انقر للاختيار")
- سيتم الرفع تلقائياً ✅

**ب) النقر والاختيار:**
- اضغط على زر "رفع الصور"
- اختر الملف من جهازك
- سيتم الرفع تلقائياً ✅

#### خطوة 3: أضف النص البديل
- املأ حقل "النص البديل للشعار" بالعربي
- مثال: "شعار شركة لوميرا للرخام"
- (اختياري) أضف بباقي اللغات بتبديل اللغة من الأعلى

#### خطوة 4: (اختياري) ارفع لوجو الوضع الليلي
- إذا كان لوجوك غامق، ارفع نسخة فاتحة للوضع الليلي
- نفس الخطوات: اسحب أو انقر واختر

#### خطوة 5: احفظ
- اضغط **"حفظ التغييرات"** في أسفل الصفحة
- ستظهر رسالة نجاح ✅

#### خطوة 6: شاهد النتيجة
- افتح الصفحة الرئيسية
- سيظهر اللوجو في Navbar و Footer فوراً!

---

### 🔄 تعديل أو حذف اللوجو

**لتغيير اللوجو:**
- ارفع لوجو جديد → سيستبدل القديم تلقائياً

**لحذف اللوجو:**
- اضغط على زر ❌ بجانب الصورة
- سيعود للوضع الافتراضي (حرف L)

---

## 🎨 توصيات التصميم

### مقاسات اللوجو المثالية:
- **Navbar Logo:** 200x200 بكسل (يُعرض بحجم 40x40)
- **Footer Logo:** 200x200 بكسل (يُعرض بحجم 48x48)
- **Favicon:** 32x32 أو 48x48 بكسل (ICO format)

### التنسيقات المدعومة:
- PNG (الأفضل - يدعم الشفافية)
- SVG (قابل للتكبير بدون فقدان الجودة)
- JPG (للوجو بخلفية)
- WEBP (الأصغر حجماً)

### نصائح:
1. **استخدم PNG مع خلفية شفافة** للتكيف مع كل الخلفيات
2. **لوجو الوضع الليلي:** إذا كان لوجوك غامق، ارفع نسخة فاتحة للوضع الليلي
3. **التحسين:** ضغط الصورة قبل الرفع (استخدم TinyPNG)
4. **النسبة:** حافظ على نسبة 1:1 (مربع) أو أفقي بنسبة 16:9

---

## 🔄 الوضع الليلي (Dark Mode)

### كيف يعمل؟
```typescript
src={siteSettings.darkModeEnabled && siteSettings.darkModeLogoUrl 
  ? siteSettings.darkModeLogoUrl 
  : siteSettings.logoUrl
}
```

**المنطق:**
1. إذا كان الوضع الليلي **مفعّل** (`darkModeEnabled = true`)
2. **وَ** تم رفع لوجو للوضع الليلي (`darkModeLogoUrl` موجود)
3. **→ اعرض** لوجو الوضع الليلي
4. **وإلا** اعرض اللوجو العادي

**مثال:**
- لوجو عادي: ارفع مباشرة من منطقة "شعار الشركة"
- لوجو ليلي: ارفع مباشرة من منطقة "شعار الوضع الليلي"
- عند تفعيل Dark Mode → يتبدل تلقائياً

---

## 🎯 المميزات الجديدة

### ✨ رفع مباشر بدون خطوات إضافية
- **قبل:** رفع الصورة → نسخ الرابط → لصقه في الإعدادات (3 خطوات)
- **الآن:** اسحب الصورة مباشرة في صفحة الإعدادات → تم! (خطوة واحدة)

### 🖱️ واجهة سهلة
- منطقة رفع واضحة بحدود منقطة
- دعم Drag & Drop (السحب والإفلات)
- أيقونة رفع واضحة
- رسائل توجيهية

### 👁️ معاينة محسّنة
- معاينة منفصلة للوضع العادي والليلي
- خلفية رمادية للوجو الشفاف
- حجم مناسب للمعاينة

### ❌ حذف سهل
- زر X واضح لحذف أي لوجو
- لا حاجة لحذف الرابط يدوياً

---

## 🌐 دعم متعدد اللغات

### النص البديل (Alt Text):
```typescript
alt={
  locale === 'ar' ? siteSettings.logoAltAr || 'شعار الشركة' :
  locale === 'es' ? siteSettings.logoAltEs || 'Logo de la Empresa' :
  locale === 'fr' ? siteSettings.logoAltFr || 'Logo de l\'Entreprise' :
  siteSettings.logoAlt || 'Company Logo'
}
```

**الفوائد:**
- ✅ تحسين SEO
- ✅ إمكانية الوصول للمكفوفين (Screen Readers)
- ✅ يظهر عند فشل تحميل الصورة

---

## 🧪 الاختبار

### السيناريوهات المختبرة:
1. ✅ رفع لوجو مباشرة (Drag & Drop)
2. ✅ رفع لوجو بالنقر (Click & Select)
3. ✅ عرض اللوجو في Navbar
4. ✅ عرض اللوجو في Footer
5. ✅ معاينة فورية بعد الرفع
6. ✅ تبديل اللغة → يتغير النص البديل
7. ✅ تفعيل Dark Mode → يتبدل اللوجو (إذا تم رفع لوجو ليلي)
8. ✅ حذف اللوجو → يظهر Fallback (حرف L)
9. ✅ رفع Favicon → يظهر في المعاينة

### حالات الـ Fallback:
- إذا لم يتم رفع لوجو → يظهر gradient box مع حرف "L"
- إذا فشل تحميل الصورة → يختفي ويظهر النص البديل
- اسم الشركة دائماً يظهر بجانب اللوجو

---

## 📊 الحالة الحالية

### ✅ مكتمل:
1. Database schema + migration
2. Admin UI لرفع وإدارة اللوجو
3. TypeScript interfaces
4. Navbar integration
5. Footer integration
6. Dark Mode support
7. Multi-language alt text
8. Preview في الإعدادات

### 🔄 المطلوب من المستخدم:
1. رفع لوجو الشركة (اسحب وأفلت مباشرة!)
2. كتابة النص البديل بالعربي
3. (اختياري) رفع لوجو للوضع الليلي
4. (اختياري) رفع favicon

**لا تحتاج:**
- ❌ الذهاب لصفحة "رفع الصور" المنفصلة
- ❌ نسخ ولصق الروابط
- ❌ خطوات معقدة

**فقط:**
- ✅ اسحب اللوجو → أفلته → تم!

---

## 📁 الملفات المُعدّلة

### ملفات جديدة:
- `prisma/migrations/[timestamp]_add_logo_branding/migration.sql`

### ملفات مُعدّلة:
1. `prisma/schema.prisma` (+7 fields)
2. `src/app/admin/settings/page.tsx` (+98 lines - قسم اللوجو)
3. `src/lib/settings.ts` (+7 interface fields + mapping)
4. `src/components/layout/Navbar.tsx` (Logo rendering logic)
5. `src/components/layout/Footer.tsx` (Logo rendering logic)

---

## 🎯 الخطوات التالية

### للمستخدم:
1. رفع لوجو الشركة الفعلي
2. تحديد النصوص البديلة بكل اللغات
3. (اختياري) رفع لوجو للوضع الليلي إذا كان مختلف

### للتطوير المستقبلي (اختياري):
- [ ] صفحة رفع مخصصة للوجو في الإعدادات (بدلاً من الانتقال لصفحة رفع الصور)
- [ ] Crop tool لتعديل اللوجو أثناء الرفع
- [ ] دعم رفع عدة أحجام (favicon, logo-sm, logo-lg)
- [ ] تحسين تلقائي للصور المرفوعة

---

## 📞 الدعم

**إذا واجهت مشكلة:**
1. **الصورة لا ترفع:**
   - تأكد من الصيغة (PNG, JPG, WEBP, SVG)
   - تأكد من حجم الملف (أقل من 5MB)
   - جرّب تصغير الصورة

2. **اللوجو لا يظهر:**
   - تحقق من المعاينة في صفحة الإعدادات
   - تأكد من الضغط على "حفظ التغييرات"
   - امسح cache المتصفح (Ctrl+Shift+R)

3. **اللوجو مقطوع أو مشوه:**
   - استخدم صورة مربعة (1:1) أو أفقية
   - تأكد من جودة الصورة الأصلية

4. **زر الرفع لا يعمل:**
   - تحقق من اتصال الإنترنت
   - راجع console المتصفح (F12)
   - أعد تحميل الصفحة

---

**تم إعداده بواسطة:** GitHub Copilot  
**التاريخ:** 2024  
**الحالة:** ✅ جاهز للاستخدام
