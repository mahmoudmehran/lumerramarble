# إعداد Google reCAPTCHA v3

## نظرة عامة
تم إضافة Google reCAPTCHA v3 لحماية نموذج طلب عرض السعر من الروبوتات والطلبات الوهمية. reCAPTCHA v3 غير مرئي للمستخدمين ولا يتطلب منهم حل أي تحديات.

## كيفية الحصول على مفاتيح reCAPTCHA

### 1. إنشاء حساب reCAPTCHA
1. انتقل إلى: https://www.google.com/recaptcha/admin
2. سجل الدخول بحساب Google الخاص بك
3. انقر على زر "+" لإنشاء موقع جديد

### 2. إعدادات الموقع
- **Label**: اسم موقعك (مثال: Lumerra Marble)
- **reCAPTCHA type**: اختر **reCAPTCHA v3**
- **Domains**: أضف نطاقات موقعك:
  - `localhost` (للتطوير)
  - نطاق الإنتاج الخاص بك (مثال: `lumerramarble.com`)
- **Owners**: أضف بريدك الإلكتروني
- اقبل شروط الخدمة

### 3. نسخ المفاتيح
بعد إنشاء الموقع، ستحصل على:
- **Site Key** (المفتاح العام) - يُستخدم في الواجهة الأمامية
- **Secret Key** (المفتاح السري) - يُستخدم في الخادم

### 4. إضافة المفاتيح إلى ملف .env
افتح ملف `.env` وأضف المفاتيح:

```env
# Google reCAPTCHA v3
NEXT_PUBLIC_RECAPTCHA_SITE_KEY="your-site-key-here"
RECAPTCHA_SECRET_KEY="your-secret-key-here"
```

**ملاحظة مهمة:**
- استبدل `your-site-key-here` بالمفتاح العام الخاص بك
- استبدل `your-secret-key-here` بالمفتاح السري الخاص بك
- لا تشارك المفتاح السري مع أحد
- لا تضف ملف `.env` إلى Git

## كيفية العمل

### في الواجهة الأمامية (Frontend)
1. عند تحميل صفحة النموذج، يتم تحميل سكريبت reCAPTCHA
2. عند النقر على زر "إرسال الطلب"، يتم:
   - تنفيذ reCAPTCHA في الخلفية
   - الحصول على token من Google
   - التحقق من الـ token عبر API
   - إذا نجح التحقق، يتم إرسال النموذج

### في الخادم (Backend)
1. استقبال الطلب مع reCAPTCHA token
2. التحقق من الـ token مع خادم Google
3. فحص النقاط (Score):
   - 1.0 = مستخدم حقيقي جداً
   - 0.5 = حد مقبول
   - 0.0 = روبوت على الأرجح
4. إذا كانت النقاط أقل من 0.5، يتم رفض الطلب

## ميزات الحماية المضافة

### 1. التحقق من البيانات المحسّن
- **رقم الهاتف**: 
  - يقبل صيغ دولية مختلفة
  - الحد الأدنى 8 أرقام
  - يدعم: +20 123 456 7890, 01234567890, +1-234-567-8900

- **البريد الإلكتروني**:
  - التحقق من الصيغة الصحيحة
  - رفض البريد الوهمي (test@test, fake@fake, example@example)

- **الاسم**:
  - الحد الأدنى حرفين
  - التحقق من النص الصحيح

### 2. reCAPTCHA v3
- حماية غير مرئية من الروبوتات
- نظام نقاط (0.0 - 1.0)
- لا يزعج المستخدمين الحقيقيين

### 3. Rate Limiting
- تحديد عدد الطلبات من نفس IP
- منع الطلبات المتكررة (Spam)
- حماية من هجمات DDoS البسيطة

### 4. Input Sanitization
- تنظيف جميع المدخلات قبل الحفظ
- منع XSS و SQL Injection
- حماية قاعدة البيانات

## اختبار النظام

### للتطوير (Development)
1. شغل السيرفر: `npm run dev`
2. افتح: http://localhost:3002/ar/quote
3. حاول ملء النموذج:
   - أدخل بيانات صحيحة ← يجب أن يعمل
   - أدخل رقم هاتف خاطئ ← سيظهر خطأ
   - أدخل بريد test@test.com ← سيظهر خطأ
   - أدخل اسم حرف واحد ← سيظهر خطأ

### للإنتاج (Production)
1. تأكد من إضافة نطاق الإنتاج في إعدادات reCAPTCHA
2. قم بنشر الموقع
3. اختبر النموذج على الموقع المباشر

## تعطيل reCAPTCHA مؤقتاً

إذا أردت تعطيل reCAPTCHA للاختبار:
1. احذف أو اترك فارغاً `NEXT_PUBLIC_RECAPTCHA_SITE_KEY` في ملف `.env`
2. سيعمل النموذج بدون reCAPTCHA

## الأمان في الإنتاج

### توصيات هامة:
1. ✅ استخدم HTTPS دائماً
2. ✅ احفظ المفتاح السري بشكل آمن
3. ✅ راقب سجلات الطلبات المرفوضة
4. ✅ راجع تقارير reCAPTCHA في لوحة تحكم Google
5. ✅ اضبط حد النقاط (threshold) حسب احتياجاتك

### مراقبة الأداء:
- راجع لوحة تحكم reCAPTCHA بانتظام
- تحقق من معدل الطلبات المرفوضة
- اضبط الحد الأدنى للنقاط إذا لزم الأمر (حالياً 0.5)

## الدعم والمساعدة

### موارد مفيدة:
- [وثائق reCAPTCHA v3](https://developers.google.com/recaptcha/docs/v3)
- [أفضل الممارسات](https://developers.google.com/recaptcha/docs/faq)
- [لوحة تحكم reCAPTCHA](https://www.google.com/recaptcha/admin)

### المشاكل الشائعة:

**المشكلة**: رسالة "reCAPTCHA not loaded"
**الحل**: تأكد من إضافة NEXT_PUBLIC_RECAPTCHA_SITE_KEY في .env

**المشكلة**: "Invalid site key"
**الحل**: تحقق من أن المفتاح صحيح وأن النطاق مضاف في إعدادات reCAPTCHA

**المشكلة**: جميع الطلبات مرفوضة
**الحل**: قلل حد النقاط (threshold) من 0.5 إلى 0.3 في `/api/verify-recaptcha/route.ts`

## الخلاصة

النظام الآن محمي بـ:
- ✅ Google reCAPTCHA v3 (حماية من الروبوتات)
- ✅ Rate Limiting (منع Spam)
- ✅ Input Validation (التحقق من البيانات)
- ✅ Input Sanitization (تنظيف البيانات)

هذا يوفر توازن جيد بين الأمان وسهولة الاستخدام! 🎉
